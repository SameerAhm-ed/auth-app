# Database (MSSQL) — copy to .env.local and fill in real values.
# .env.local is gitignored; never commit real credentials.

# Main database (AM4 / AM14 / AM15)
DB_USER=
DB_PASSWORD=
DB_SERVER=
DB_NAME=
DB_PORT=1433

# AM5 database (separate server / credentials)
DB_AM5_USER=
DB_AM5_PASSWORD=
DB_AM5_SERVER=
DB_AM5_NAME=
DB_AM5_PORT=1433
